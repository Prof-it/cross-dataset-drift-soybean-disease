# Runtime dependencies (mirror of pyproject.toml [project.dependencies]).
# Convenience file for Colab / pip installs: `pip install -r requirements.txt`.

# Core deep-learning stack
torch>=2.1.0
torchvision>=0.16.0
timm>=1.0.15

# Data and numerics
numpy>=1.24.0
pandas>=2.0.0
Pillow>=10.0.0

# Metrics and statistics
scikit-learn>=1.3.0
scipy>=1.11.0
statsmodels>=0.14.0

# Configuration, logging, progress
PyYAML>=6.0
tqdm>=4.65.0
tensorboard>=2.14.0

# Figures
matplotlib>=3.7.0
seaborn>=0.12.0

# Note: Paper 2 (explainability) will add grad-cam, captum, umap-learn, opencv-python.
